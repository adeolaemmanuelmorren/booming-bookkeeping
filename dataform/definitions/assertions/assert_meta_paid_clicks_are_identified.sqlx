config {
  type: "assertion",
  description: "Meta paid page views must have fresh campaign and ad identifiers in the landing URL.",
  tags: ["assertion", "attribution", "meta"],
}

select *
from ${ref("stg_page_views")}
where utm_source = 'meta'
  and utm_medium = 'paid'
  and not is_identified_meta_ad_click
